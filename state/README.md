# Captured state

The capture scripts write exact installed package and tool versions here. These
local snapshots document a build; `config.psd1` and `packages.txt` remain the
desired-state inputs. Generated `*.txt` inventories are ignored by Git.
